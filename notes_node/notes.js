console.log("starting notes.js");

// all files have availability or access to avariable called module.
// it has an important property called exports
// console.log(module);

module.exports.addNumbers = function(x,y){

    return x+y;
} ;
